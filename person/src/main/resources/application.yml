spring:
  application:
    name: person-service
  datasource:
    url: jdbc:postgresql://localhost:5432/product
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080