spring:
  config:
    import: "optional:configserver:http://configserver:8888/"
  cloud:
    config:
      name: person-service
      profile: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"
